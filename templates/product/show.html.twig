{% extends 'base.html.twig' %}

{% block body %}
    <h1>{{ product.title }}</h1>
    <p>Price: {{ product.price }}</p>
    <hr>
    <p>{{ product.descripiton }}</p>
{% endblock %}

{% block title %}
    {{ product.title }}
{% endblock %}

